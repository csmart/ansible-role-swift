---
swift_account_rings_min_part_hours: 1
swift_account_rings_part_power: 10
swift_account_rings_replicas: 3
swift_account_rings:
  account:
    part_power: "{{ swift_account_rings_part_power }}"
    replicas: "{{ swift_account_rings_replicas }}"
    min_part_hours: "{{ swift_account_rings_min_part_hours }}"
