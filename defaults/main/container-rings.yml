---
swift_container_rings_min_part_hours: 1
swift_container_rings_part_power: 10
swift_container_rings_replicas: 3
swift_container_rings:
  container:
    part_power: "{{ swift_container_rings_part_power }}"
    replicas: "{{ swift_container_rings_replicas }}"
    min_part_hours: "{{ swift_container_rings_min_part_hours }}"
